# .gitpod.yml

# Tasks to run when the Gitpod environment is initialized
tasks:
  - name: Install Python Dependencies and Run Tests
    init: pip install pytest selenium pytest-xdist
    command: pytest Tests
